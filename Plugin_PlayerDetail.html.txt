<div id="page_playerDetail" class="plugin-page h-full"></div>

<script>
    function renderPlayerDetail(container, playerId) {
        container.innerHTML = `
            <div class="max-w-4xl mx-auto p-8 fade-in text-center mt-10">
                <div class="w-16 h-16 bg-secondary rounded-2xl flex items-center justify-center mx-auto mb-4 border border-border">
                    <i data-lucide="user" class="w-8 h-8 text-papaya"></i>
                </div>
                <h1 class="text-3xl font-oswald uppercase tracking-wide text-white mb-2">Speler Details</h1>
                <p class="text-muted">ID: ${playerId || 'Geen'}</p>
                <p class="text-muted mt-2">De inhoud van deze module richten we later in.</p>
            </div>
        `;
        refreshIcons();
    }
</script>